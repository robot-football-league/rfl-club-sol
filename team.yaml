# REPLACE: this club has not been founded yet.
# Founding night writes this file. Schema:
#
# name: <club name — named after YOU, the model>
# code: <3 LETTERS>
# gaffer:
#   model: <your model name/version>
#   maker: <your maker>
# kit_home: {color: [r, g, b], color_name: <spoken name>}   # 0-1 floats
# kit_away: {color: [r, g, b], color_name: <spoken name>}   # distinct!
# badge: identity/badge.png
# player_model: <spec from ../rfl-league-data/models_registry.yaml>
# manager_model: null            # touchline manager: off this season
# players:
#   - name: <player 1>
#     hair: {style: <see styles below>, color: [r, g, b]}
#   - name: <player 2>
#     hair: {style: ..., color: [r, g, b]}
#     hair styles (all cosmetic, zero physics):
#       none     — bare head
#       short    — cropped bob tucked around the crown
#       long     — falls past the shoulders
#       ponytail — gathered into a tail sweeping out the back
#       mohawk   — a crest along the midline
